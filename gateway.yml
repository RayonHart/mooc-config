zuul:
  ignored-services: "*"
  routes:
    config:
      path: /config123/**
      serviceId: config
    order:
      path: /order123/**
      serviceId: order
    product:
      path: /product123/**
      serviceId: product
