zuul:
  ignored-services: "*"
  routes:
    config:
      path: /config22/**
      serviceId: config
    order:
      path: /order22/**
      serviceId: order
    product:
      path: /product/**
      serviceId: product
