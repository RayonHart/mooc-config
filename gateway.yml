zuul:
  ignored-services: "*"
  routes:
    config:
      path: /config12/**
      serviceId: config
    order:
      path: /order12/**
      serviceId: order
    product:
      path: /product12/**
      serviceId: product
